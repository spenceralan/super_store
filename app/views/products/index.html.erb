<div id="product_index">

  <h3>There <%= @products.length == 1 ? "was" : "were" %> <%= pluralize @products.length, "result" %></h3>

  <% @products.each do |product| %>
    <h3><%= link_to product.name, product_path(product) %></h3>
  <% end %>
  
  <%= paginate @products %>
</div>